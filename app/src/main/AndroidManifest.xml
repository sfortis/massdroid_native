<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Declares optional support for Android Automotive OS (AAOS). The system
         uses this together with the meta-data inside <application> below to
         surface MassDroid in the car's media center. required="false" so the
         APK still installs on regular phones. -->
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="false" />

    <application
        android:name=".MassDroidApp"
        android:allowBackup="true"
        android:appCategory="audio"
        android:fullBackupContent="@xml/full_backup_content"
        android:dataExtractionRules="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.MassDroidV2">

        <!-- Tells AAOS this is a media app. The XML resource declares
             <uses name="media"/>, which is what the AAOS Media Center reads
             to surface MassDroid as a media source. The meta-data name
             "com.android.automotive" is the AAOS path; the projected Android
             Auto equivalent ("com.google.android.gms.car.application") is
             intentionally NOT used here, that's a different host. -->
        <meta-data
            android:name="com.android.automotive"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.MassDroidV2.Splash">
            <!-- distractionOptimized intentionally NOT set: that flag is only
                 valid for Car App Library CarAppActivity, and adding it to a
                 regular media UI activity is a Play rejection trigger (rule
                 DD-2). The car media center is the host while driving; this
                 activity is parked-only and opens only when the user taps the
                 app icon while parked. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Music Assistant OAuth callback (Home Assistant SSO).
                 Uses the official MA mobile-app scheme because the MA server's
                 OAuth redirect allowlist accepts only this URI prefix today. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="musicassistant" android:host="auth" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <service
            android:name=".service.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback|connectedDevice">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
            <!-- The AAOS Media Center filters MediaBrowserServices by this
                 opt-in flag. Without it, the service shows up as "non media
                 template app" in com.android.car.media's MediaSource log and
                 the app never appears in the car's media quadrant. Verified
                 against system LocalMediaPlayer in the AAOS-15 emulator. -->
            <meta-data
                android:name="androidx.car.app.launchable"
                android:value="true" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
